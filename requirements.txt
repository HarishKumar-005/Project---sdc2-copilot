# ── Core data engine ──────────────────────────────────
polars>=1.44.1,<2.0.0
pandas>=2.0.0
pyarrow>=17.0.0

# ── UI & Authentication ───────────────────────────────
streamlit>=1.45.0
authlib>=1.3.0

# ── Workflow orchestration (thin wrapper) ─────────────
prefect>=3.0.0

# ── LLM providers ────────────────────────────────────
google-genai>=2.8.0
groq>=1.4.0

# ── Configuration & resilience ────────────────────────
pydantic>=2.0.0
pydantic-settings>=2.0.0
python-dotenv>=1.0.0
tenacity>=9.0.0

# ── Database & Storage (V2) ───────────────────────────
psycopg[binary]>=3.2.0

# ── Testing ───────────────────────────────────────────
pytest>=8.0.0

# ── Operational API & Authentication (V2.6) ───────────
fastapi>=0.115.0
uvicorn>=0.30.0
httpx>=0.27.0
pyjwt[crypto]>=2.8.0